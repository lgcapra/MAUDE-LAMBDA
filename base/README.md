Definitions for data structures and iterables. 

- `COLLECTIONS.maude`: interfaces and views used for representing collections
  in higher-order operations
- `ELIST.maude`: lists of the form `[a,b,c]` (enclosed in brackets). 
- `MONOID.maude`: A functional theory for (variants of) monoids. 
