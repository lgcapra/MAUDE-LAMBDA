Definition of higher-order functions using parametrized modules. In this approach, the function 
to be applied in higher-order combinators must be defined in a separate module. 

In the end of each file there are some examples of use. 

- `ITERABLE-MAP.maude`: General definitions for a `map`. 
- `MONOID-MAP.maude`: A simpler version using monoids. 
